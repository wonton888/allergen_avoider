<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css" type="text/css">
        <link rel="stylesheet" href="css/reset.css">
        <link rel="stylesheet" href="css/styles.css" media="screen" title="no title" charset="utf-8">
        <script src="js/prefixfree.min.js"></script>
        <title>Allergen Avoider</title>
    </head>
    <body>
        <div class="container">
            <form class="right" action="/admin">
                <button type="submit">Admin</button>
            </form>
            <div class="jumbotron">
                <h1>Allergen Avoider</h1>
                <h3>Select allergens to avoid below for a listing of suitable restaurants</h3>
            </div>
            <dl class="dropdown">
                <dt>
                <a href="#">
                  <span class="hida">Select menu options</span>
                  <p class="multiSel"></p>
                </a>
                </dt>

                <dd>
                    <div class="mutliSelect">
                        <form action="/options" method="post">
                            <ul>
                                {% for option in options %}
                                <li>
                                    <input type="checkbox" name="option_names[]" value="{{ option.getName }}"/>
                                    {{ option.getName }}
                                </li>
                                {% endfor %}
                            </ul>
                            <button type="submit">Filter Restaurants</button>
                        </form>
                    </div>
                </dd>
            </dl>
        </div>
        <script src='http://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js'></script>
        <script src="js/scripts.js"></script>
    </body>
</html>
